<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>{{ title }}</title>
{% if description %}<meta name="description" content="{{ description }}" />{% endif %}
{% if canonical %}<link rel="canonical" href="{{ canonical }}" />{% endif %}
<meta name="robots" content="index, follow" />

<!-- Open Graph -->
<meta property="og:type" content="website" />
{% if canonical %}<meta property="og:url" content="{{ canonical }}" />{% endif %}
<meta property="og:title" content="{{ title }}" />
{% if description %}<meta property="og:description" content="{{ description }}" />{% endif %}
<meta property="og:site_name" content="Refueler" />

<!-- Twitter / X card -->
<meta name="twitter:card" content="summary" />
<meta name="twitter:title" content="{{ title }}" />
{% if description %}<meta name="twitter:description" content="{{ description }}" />{% endif %}

<!-- Fonts: Satoshi · DM Sans · IBM Plex Mono -->
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,300..700;1,9..40,300..700&display=swap" rel="stylesheet" />
<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet" />
<link rel="stylesheet" href="https://api.fontshare.com/v2/css?f[]=satoshi@400,500,600,700&display=swap" />

<!-- Global styles: tokens, reset, nav, footer -->
<link rel="stylesheet" href="/assets/css/global.css" />

<!-- Icons -->
<link rel="icon" type="image/svg+xml" href="/assets/images/favicon.svg" />
<link rel="icon" type="image/png" sizes="32x32" href="/assets/images/favicon-32.png" />
<link rel="apple-touch-icon" sizes="180x180" href="/assets/images/apple-touch-icon.png" />
<meta name="theme-color" content="#1A1A1A" />

{{ extraHead | safe }}

<!-- Theme: applied before first paint.
     Detection: document.documentElement.dataset.theme === 'carbon'
     Persistence: cookie rs-theme scoped to .refueler.io (30-day rolling)
     Paper is default on public site. Carbon is user toggle only. -->
<script>
  (function () {
    function getCookie(name) {
      var m = document.cookie.match('(?:^|; )' + name + '=([^;]*)');
      return m ? decodeURIComponent(m[1]) : null;
    }
    function setCookie(name, value) {
      var expires = new Date(Date.now() + 30 * 864e5).toUTCString();
      document.cookie = name + '=' + encodeURIComponent(value) + '; expires=' + expires + '; path=/; domain=.refueler.io; SameSite=Lax';
    }
    function applyTheme(theme) {
      var root = document.documentElement;
      var pill = document.getElementById('themePill');
      if (theme === 'carbon') {
        root.dataset.theme = 'carbon';
        if (pill) pill.textContent = 'Carbon / Paper';
      } else {
        delete root.dataset.theme;
        if (pill) pill.textContent = 'Paper / Carbon';
      }
    }
    window.toggleTheme = function () {
      var current = getCookie('rs-theme') || 'paper';
      var next = current === 'paper' ? 'carbon' : 'paper';
      setCookie('rs-theme', next);
      applyTheme(next);
      {% if stripeThemeRemount %}if (typeof remountStripeForTheme === 'function') remountStripeForTheme();{% endif %}
    };
    applyTheme(getCookie('rs-theme') || 'paper');
    window.toggleNavDrawer = function () {
      var h = document.getElementById('site-header');
      if (h) h.classList.toggle('nav-open');
    };
    document.addEventListener('click', function (e) {
      var h = document.getElementById('site-header');
      if (h && h.classList.contains('nav-open') && !h.contains(e.target)) {
        h.classList.remove('nav-open');
      }
    });
  }());
</script>
