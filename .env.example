# .env.example — Refueler
# Updated: CC-64 · 8 July 2026
#
# Copy this file to .env.local for local development.
# NEVER commit .env.local or any file containing live secrets to GitHub.
# .env.local is in .gitignore — verify before first commit.
#
# Variable prefixes:
#   VITE_       — injected at build time by Vite / Cloudflare Pages (client-visible)
#   no prefix   — server-side only (Supabase edge function secrets, never client-visible)
#
# To set edge function secrets:
#   supabase secrets set BLINK_API_KEY=<value> --project-ref tihgvdokeofnjxjkenmm

# ---------------------------------------------------------------------------
# Supabase
# ---------------------------------------------------------------------------

# Project URL — safe to expose (needed by client SDK)
VITE_SUPABASE_URL=https://tihgvdokeofnjxjkenmm.supabase.co

# Anon key — safe to expose (enforced by RLS). Used in client-side JS.
VITE_SUPABASE_ANON_KEY=eyJ...

# Service role key — NEVER expose client-side. Edge functions only.
# Set via: supabase secrets set SUPABASE_SERVICE_ROLE_KEY=<value>
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# ---------------------------------------------------------------------------
# Blink / Lightning BOLT11
# ---------------------------------------------------------------------------

# Blink API key — edge function only. Never in client bundle.
# Obtain from: dashboard.blink.sv → API keys
# Set via: supabase secrets set BLINK_API_KEY=<value>
BLINK_API_KEY=

# Svix webhook signing secret — used by blink-webhook to verify HMAC-SHA256 signature.
# Obtain from: dashboard.blink.sv → Webhooks → Signing secret
# Set via: supabase secrets set SVIX_SIGNING_SECRET=<value>
SVIX_SIGNING_SECRET=

# ---------------------------------------------------------------------------
# Cloudflare
# ---------------------------------------------------------------------------

# Analytics Worker auth token — used by refueler-analytics worker.
ANALYTICS_WORKER_TOKEN=

# ---------------------------------------------------------------------------
# Email (Resend)
# ---------------------------------------------------------------------------

# Resend API key — Supabase SMTP provider. Configured via Supabase dashboard.
# Used for: noreply@refueler.io magic link delivery via send.refueler.io subdomain.
RESEND_API_KEY=re_...

# ---------------------------------------------------------------------------
# Ecash (future — internal mint only)
# ---------------------------------------------------------------------------

# Internal mint URL — DO NOT POPULATE until refueler-mint is live.
# Two planning sessions (A + B) required before deployment.
# REFUELER_INTERNAL_MINT_URL_PENDING=

# ---------------------------------------------------------------------------
# Development flags
# ---------------------------------------------------------------------------

# Set to 'true' to skip Blink API calls in local dev and return a mock invoice.
# Never set in production.
VITE_MOCK_PAYMENTS=false
